# 安全说明

Uroad Log Analyzer 的公开仓库只保存脱敏后的应用代码、合成测试数据和通用文档；公司服务域名与运行凭据必须由部署环境注入。

## 不得提交的内容

- 真实 `settings.json`、令牌、Cookie、OAuth 状态或请求头；
- 公司内部服务域名、下载域名和内部网络拓扑；
- 真实 VIN、原始日志、CAN 数据、分析 JSON 和 HTML 报告；
- 飞书消息、群聊、用户、应用或文档标识；
- 登录二维码、个人简历、内部压缩包和运行目录。

`.gitignore` 只是最后一道防线。提交前仍需检查暂存区和完整 diff；如果敏感信息曾进入 Git 历史，应立即停止推送、轮换凭据，并由仓库管理员清理历史。

## 漏洞与事故报告

不要在公开 Issue 中粘贴日志、配置或可复现凭据。请通过团队既定的内部安全渠道联系“安全响应负责人”；具体角色和替补联系方式由 [`MAINTAINERS.md`](MAINTAINERS.md) 维护。

报告至少包括：受影响版本、发现时间、影响范围、脱敏复现步骤和建议处置。凭据本身不得附在报告正文中。

## 公开仓库边界

公开版本必须使用合成样例，并在提交历史中持续扫描凭据、真实 VIN、个人文件和公司域名。部署端点通过环境变量提供，不得回填到源码。本仓库当前没有项目级开源许可证；公开可见不等于授予复制、修改或再分发权利。
